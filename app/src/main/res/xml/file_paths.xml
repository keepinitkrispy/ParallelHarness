<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Staged update APKs shared with the system package installer. -->
    <external-files-path name="update" path="update/" />
    <!-- Files staged from the workspace for system share / open-with intents. -->
    <cache-path name="shared" path="shared/" />
</paths>
