<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only host activity for the instrumentation tests that need a real
     window (see app/src/androidTest). Never part of a release build. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.androidharness.app.browser.TestHostActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
    </application>
</manifest>
